import { MIME_TYPES } from "@mantine/dropzone";

export const ERROR_LENGTH_MS = 5000;

export const SUPPORTED_FORMATS = [MIME_TYPES.png, MIME_TYPES.jpeg];
export const MAX_FILE_SIZE = 5 * (10 ** 6);

export const CDN_URL = "https://cdn.craftex.dev/plugins/";
