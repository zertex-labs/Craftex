import React from "react";
import { AiFillCodeSandboxCircle, AiOutlineSearch } from "react-icons/ai";

const SidebarComponent = () => <React.Fragment></React.Fragment>;

export default React.memo(SidebarComponent);
